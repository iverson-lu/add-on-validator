[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "addon-catalog"
version = "0.1.0"
description = "HP add-on catalog downloader and analyzer"
authors = [
  { name = "AutoGenerated" }
]
readme = "readme.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]
